# 永夜回响 · 余烬之境

React 19 + TypeScript + Vite 7 + Three.js 的卡牌肉鸽游戏。当前入口是主世界探索（`src/world/`），
卡牌战斗原型保留在 `src/App.tsx` / `src/engine.ts`，尚未接入地图。

## 命令

```sh
npm run dev      # http://127.0.0.1:5173/#/world
npm test         # 223 项，node --experimental-strip-types --test
npm run build    # tsc -b && vite build
```

## 当前状态（2026-09-21）

主世界已扩至 **2315 格**（面积 ×3），地形有噪声驱动的起伏，植被重做过，
每个地貌有独立建筑造型且已统一放大 1.5 倍。细节层已补齐：分层屋面、尖顶旗饰、
窗棂十字、台阶栏杆、围栏、附属棚屋。12 个支线地点与 12 个隐秘据点各占 3 格，
是「前庭 + 两间偏屋」的小建筑群（偏屋造型随地貌而变）；12 个路边奇遇保持单格。
测试全绿，构建干净。

每个地貌另有 **2~3 只自由活动的动物**（共 29 种，模型来自 CC0 素材，见下）。
8 个敌对种有自发光眼睛，陆生动物走对角步态（腿前后摆、抬蹄、身体随步点起伏）。

**第一章战斗已从「单场 demo」变成一条完整的 run**：五场遭遇战、血量继承、每场胜利后回血
9–15、主副牌组可调换、胜负结算与进度存档（`#/battle`）。此前 `BattleDemo.tsx` 把 encounter
和牌组写死成 `ch1-1` + `blade`，萤火 / 商队首领 / 头狼 / 余烬守望者四只怪和整副长明壁垒
**玩家一次也碰不到**。boss 的解锁奖励仍是空的，见下面「下一轮」。

### 待办

1. **更多隐秘据点与路边奇遇**。`worldStories.ts` 的 `STORIES`/`ENCOUNTERS` 是
   每地貌单例，需改为数组。**现有 id 必须保持稳定**（`side-cliff`、`hidden-volcano`
   等被章节链和玩家存档依赖），新地点只能追加新 id。连带要改
   `WorldLocation.tsx` / `WorldJournal.tsx` 里 `ENCOUNTERS[site.biome]` 的取法。
2. **主世界河流没有实体**。`WorldWeather.ts` 只画河面缎带，河不参与寻路也不挡路，
   建筑可以压在河上。若要"真正的河"，得让河格不可走并重划桥。
3. **敌对生物目前只是气氛**。它们不会靠近、不会挡路、不触发遭遇，只有图鉴里的
   `temperament` 标签与配色区分。要做"会追人的狼"得接 `WorldCreatures` 的 wander
   状态机与 `canEnterTile`，工程量另算。
4. **生物只有"走"，没有别的状态**。没有奔跑、受惊、进食以外的行为，转身也只是一条
   插值曲线。要做"会追人的狼"见第 3 条。

## 动手前必读的坑

这些都是这个项目里踩过的，改之前先看一眼：

- **`worldProgression.ts` 与 `WorldProgression.tsx` 只差大小写**。在 Windows 上
  `'./WorldProgression'` 会解析到 `.ts` 逻辑文件而非 `.tsx` 组件，导入必须写显式扩展名。
- **生成期用堆而非线性扫描**。`worldData.ts` 的 `connect()` 与区域遍历用
  `MinCostQueue`，且可达性结果按 `walkVersion` 缓存。改成"可续跑的增量 BFS"是错的——
  返回集合内容相同但迭代顺序不同，会导致不同的划桥位置和不同的世界。
- **平局规则是有意义的**。原 `[...open].reduce()` 用严格小于，平局取迭代序最后一个。
  堆已精确复刻，改动前先看 `priorityQueue.ts` 的注释。
- **布局摘要测试**（`worldData.test.ts` 的 `LAYOUT_DIGEST`）是海岸线与划桥的冻结基线。
  有意改地图时才重新生成，否则它一失败就说明世界被意外改动了。
- **世界尺寸用 `WORLD_SCALE` 派生**。相机钳制、阴影正交盒、雾、小地图 viewBox、
  缩放档位、天气范围全部乘以它，不要写回字面量。
- **建筑改尺寸要同步三处**：`WorldScene.ts` 的拾取盒高度表（否则点屋顶穿透）、
  信标浮空高度、以及 `WorldLighting.ts` 找灯塔光束的 `dy` 阈值。
- **生物三个文件分工，缺一不可，且名字不能只差大小写**：
  `faunaSpecies.ts`（纯物种表，只有一个 type-only import）、`worldFauna.ts`（摆放，
  只依赖 worldData）、`WorldCreatures.ts`（渲染，OBJ 加载 + 实例化 + 游荡）。
  拆开是为了让 `worldData.parseSave` 能校验 `fauna` 存档字段而不 import 摆放层
  （摆放层 import 了 worldData，会成环）。
  **这个坑已经真踩过**：一开始渲染层叫 `WorldFauna.ts`，与 `worldFauna.ts` 只差大小写，
  Write 时在 Windows 上**直接静默覆盖**了数据文件，代码整个消失。别再用这种名字。
- **配乐是谱面数据，不是音频文件**。`worldScores.ts` 是纯数据（和弦、旋律、调式、音色），
  `WorldSoundscape.ts` 现场合成。加分轨只要往 `SCORES` 里加一首，右上角面板自动列出来。
  注意 `worldScores.ts` 的 `mode` 字段是调式白名单，`soundscape.test.ts` 拿它校验每个音——
  写错一个音会直接红，这比事后用耳朵发现强。
- **纯数据必须和合成器分文件**。`WorldSoundscape.ts` 的构造函数用了 TS 参数属性
  （`constructor(private ctx: AudioContext, ...)`），而 `node --experimental-strip-types`
  是 strip-only，**加载这种语法直接抛错**。所以谱面数据拆在 `worldScores.ts` 里，
  测试只 import 那个。和 `faunaSpecies.ts` / `worldFauna.ts` 的拆法同一个理由。
- **`WorldLocation.tsx` / `WorldJournal.tsx` 用 `ENCOUNTERS[site.biome]` 取奇遇内容**，
  加第二个奇遇就会撞车，需改为按地点 id 取。
- **熔岩池用 `tile.caldera` 标记**，地形塑形与渲染共用该判据。
- **生物模型是异步加载的，`onReady` 被它挡住了**。`WorldScene.settle()` 要等
  `pendingLoads` 归零才回调 `onReady`，否则加载遮罩撤掉时动物还没出现，会看到一段
  "先没动物、再突然冒出来"。加载失败只记录并跳过（世界照常打开），
  但**每个物种的模型文件是否存在有测试兜底**——否则打错文件名会静默少一只动物。
- **导入的生物是单色平涂，颜色是按几何算出来的**。`WorldCreatures.tint()` 按高度分三段
  （最低段=腿、朝下的面=腹、其余=外套色）。头部朝向也是推断的：头颈更高的一端是头，
  四足动物上都对，但**鱼和鲸不行**（见下一条）。
- **`piranha.obj` 与 `whale.obj` 是反的，必须给 `model.yaw: Math.PI`**。这两个网格尾鳍
  比吻部还高，`headYaw()` 判反，少了 `yaw` 它们就**倒着游**——piranha / bloodfin /
  whale / bonewhale / mistwhale 五条都吃这个。`fauna.test.ts` 有一条要求共用同一网格的
  物种 `yaw` 一致，加第三种鲸时忘抄会直接红。
- **着色器里的每物种常量必须走 uniform，不能拼进源码**。29 个物种共用**一个**编译结果：
  `shaderID` 存在时 three.js 只按 `material.customProgramCacheKey()` 认程序，而它是常量，
  **`onBeforeCompile` 改过的源码根本不参与缓存键**。早先把 `minY/spanY/minZ/spanZ` 用
  `toFixed(4)` 拼进顶点着色器，后果是**第一个编译的物种（狐狸）的比例被全部 29 只共用**：
  鲸的全身落进"头部"带，秧鸡的落进任何带之外（小到 `fTop` 恒为 0，头颈完全不动）。
  现在统一走 `uFaunaBox` / `uFaunaWalk` / `uFaunaLegs` / `uFaunaEye`，程序数仍然是 1。
  加新的每物种动画量时照做，别再用模板字符串。
- **眼睛是焊进身体几何的**（追加 2 个八面体 + `aGlow` 顶点属性），不是子 mesh，
  所以 draw call 不变。用八面体而不是朝外的面片，是因为面片在动物背对镜头时会整个消失。
  有 `palette.accent` 才有眼睛，`accent` ⇔ `temperament: 'hostile'`，测试钉住了这条。
  眼睛位置全由几何推：头带取 z 的 [72%, 88%]（取前 1/5 会量到鼻梁、取前 1/3 会量到肩背，
  狗还会量到前腿，第一次就栽在这），高度 = 该带最高点 − 16% 身高，宽度 = 眼高处的最大 |x|。
  猫要特别注意：它的最高点是**竖起来的尾巴**，所以眼睛高度绝不能取整体身高的比例。
- **步态是"髋部不动、蹄子位移最大"的剪切**，不是整体平移：`below` 从髋的 0 渐变到蹄的 1，
  腿才不会从身上撕下来。步频由步幅反推（`speed*.55 / (2*stride)`），两者绑死才不会滑步，
  改 `motion.speed` 不用手动调频率。
- **水生与飞行种按"体长"归一化，不是身高**（`sizeAxis: 'length'`），否则鲸鱼按身高
  缩放会有两个半格长。游动生物的入水深度必须用**模型高度**算，不能用 `size`——
  这两个量对鲸鱼毫无关系，用 `size` 会把整条鲸沉到海面下一整个单位。
- **生物不进 `Tile`，所以布局摘要不受影响**。加生物不需要重算 `LAYOUT_DIGEST`。
- **`tileRegion` 依赖地点 id 的命名契约**。`worldRegions.ts` 从 `structure` 里取章节：
  主建筑 id 恰好等于地貌名（`camp` 归 `grass`），派生据点则是 `<kind>-<biome>` 取后缀。
  **给主建筑加带连字符的 id 会踩坑。** 这条不能靠 import LANDMARKS 解决——
  `worldRegions.ts` 只有 type-only import，加运行时依赖会与世界生成循环互引。
  早先直接把 `structure` 断言成 `Biome`，派生据点的前庭会被判成"封印区域"，
  玩家走不进去、存档还会被重置回营地。
- **派生据点的生长循环必须排在主建筑循环之后**（`worldData.ts` 的 `createWorld`）。
  两个循环的候选过滤都排除 `t.landmark`，而派生入口早有 landmark 标记，
  所以排在后面就只能捡主循环剩下的格子，主建筑 footprint 逐字节不变。调换顺序会改整张地图。
  循环里两件事不能漏：`walkVersion++`（否则可达性缓存失效，被切断的据点会静默不可达）
  和 `next.bridge=false`（否则 `WorldBridges` 会把栈桥画进建筑里）。
- **`siteFootprint()` 有记忆化，返回共享数组**，只能读不能改。标签层逐帧要读它，
  不缓存就是每帧 48 × 2315 次过滤。
- **偏屋用 `WINGS` + `scaled()` 画**。`add()` 是唯一的缩放闸口，所以 `WING_SCALE`
  会自动作用到 `window()` / `crystal()` / `lantern()` / `boat()` 上——但 `beam()`
  绕开 `add()`，不吃任何缩放（既有的桅杆、根须、帐篷杆其实都还是 1×，历史遗留）。
  新细节一律走 `add()`，不要用 `beam()`。
- **旗舰/旗饰一律不能是 `glow`**。`add()` 里 `color==='glow'` 会注册 emitter，
  灯塔光束靠 `activeSite.id==='fog' && dy>1.35` 挑最高那个，在雾港主塔的灯室之上
  再加 glow 会把光束挪走。

## 验证

项目留有 Playwright + 本机 Chrome 的无头截图管线，运行时在
`~/.cache/codex-runtimes/.../node_modules/playwright`，Chrome 在
`C:/Program Files/Google/Chrome/Application/chrome.exe`。

- `.work/world-final-perf.cjs` —— 性能基准，跑 overview/pan/zoom/walking/volcano
  五场景，输出帧时间、绘制调用、三角形数、实际 GPU 型号。**动手前后各跑一次对比。**
  （`.work/world-perf-bench.cjs` 与 `world-performance-check.cjs` 已过时，不要用。）
- `.work/world-closeup.cjs <tag> "<名称>:<格id>:<缩放>;..."` —— 按坐标拍近景。
  注意视角之间用 `;` 分隔，因为格坐标本身含逗号。
- ⚠️ `.work/run-shot.cjs` **已过时**（它等 `.bd-brief`，而塔上线后点「以此开局」是直接进塔的）。
  拍塔用 `.work/tower-shot.cjs <tag>`。
- `.work/tower-cadence.ts` —— **量塔的节奏**：DP 枚举所有路线（不是抽样），报每条路线的营火数
  分布 + 每行各类节点的占比。改 `MAP_ROWS` / `bandFor` / `FIRST_*_ROW` 之后跑它对比，
  别凭感觉说「营火够了」。`.work/tower-reveal.ts` 打开局与站定后每一行的可见情况。
- `.work/tower-campfire.ts` —— 铺一份**合法**的中段存档（`isValidRun` 只校验 path/at 的节点
  存在，不校验路线连续性），把玩家放到营火下一行，截图 / 悬停 / 走进去三连拍。要靠点击爬到
  第 9 行得先打赢六七场仗，所以这类「深处的界面」一律这么验。
  注意 `addInitScript` 里铺存档**必须加 `sessionStorage` 哨兵**，否则任何一次 `reload()`
  都会把刚铺好的存档擦掉，表现为「等了 20 秒没进塔」。
- `.work/run-e2e.cjs <tag>` —— **真的把一场打赢**：靠点击出一手牌、结束回合，然后读战果面板
  和下一场的开局血量。改回血、进度或战果面板后跑它。
- `.work/run-edges.cjs <tag>` —— 两条边界：带 1 点生命进场**打输**（确认存档被清、回到开场），
  以及**已通关的存档**（5/5、按钮禁用）；外加一份损坏存档必须被 `isValidRun` 挡下并回退。
  这三条都点不出来，但都是「只见过胜利的页面」会崩的地方。
- 注意 `.work/battle-shot.cjs` 与 `bd-*.cjs` **已过时**（指向早已不存在的 `.tour` / `.coach`），
  不要用。
- `.work/world-shot.cjs <tag> [1]` —— 整图与总览。
  （`world-final-perf.cjs` 的 volcano 场景目前是坏的：`samples:-20`、没有任何 draws，
  改动前后都一样，不是回归。）
- `.work/fauna-eyes.cjs <tag> [zoom]` —— 8 个敌对种各拍一张头颈近景，冻结在确定性出生
  姿态，两次跑可以直接 A/B。`.work/fauna-walk.cjs <物种> <帧数>` 是同一套的自然行走连拍。
- `.work/fauna-pose.cjs <物种> [zoom] [裁切宽]` —— 把一只钉在 yaw 0/90/180/270 各拍一张。
  **判断"头朝哪边"就靠它**：相机固定在 +x+z，yaw=90 时局部 +z 落在屏幕右侧，
  所以哪端是头一眼可辨。鱼和鲸的朝向 bug 就是这么查出来的。
- `.work/fauna-gait.cjs <物种> [帧数]` —— 侧对镜头 + 把 `aGait` 钉在 1 的走姿连拍。
  注意它**包住 `creatures.update` 来钉实例矩阵**：用 rAF 会和场景自己的循环抢，
  大约一半的帧会拍到没钉住的状态。也别指望 reduced-motion 下能看动画——
  `WorldScene` 在 `reduced` 时根本不推进 `elapsed`，整条时间轴是冻的。
- `.work/fauna-programs.cjs` —— 打印着色器程序数与每物种的 fauna uniform，
  用来验证上面那条"29 个物种共用一个程序"的坑没有复发。
- `.work/world-audio.cjs <曲目id> [秒数]` —— **没法"听"的时候用它**：把 AnalyserNode 接到
  主输出上，平均若干帧的频谱，列出峰值并判断每个峰是否在谱面上。改配乐后跑一次，
  比对 `matched N/N`。目前两首都是 22/22，音分误差基本在 ±5 以内。
- `.work/world-score-shot.cjs <tag> [曲目] [宽] [高]` —— 右上角声音面板关闭/展开的截图。
  注意脚本是**在出错时也 process.exit** 的：只写 `process.exitCode` 的话，浏览器没关，
  进程会一直挂着不返回。

浏览器加载有 WebGL，无头模式可正常渲染。**改完视觉务必自己截图核对**，
不要凭推断下结论——这个项目里出现过"看到河口变宽就以为已通海"的误判。

## 卡牌层（`src/cards/`）

`#/cards` 是卡牌总览页。这一整块**不 import `src/world/**` 的任何东西**，所以看牌、
改牌、跑卡牌测试都不会碰到地图生成或存档；`main.tsx` 里也只多了一个路由分支。

- **四副牌组**，各 46 张（32 种）：断罪之刃 / 燎原余烬 / 长明壁垒 / 千面回廊。
  跨组联动是一个环：刃产**余烬** → 焰烧它施加**灼烧** → 骨把灼烧与格挡变成**壁垒** →
  镜把壁垒复制成**映照** → 喂回刃。另有 10 张中立牌，每副带其中 4 张。
- **机制分两类**（`types.ts` 的 `KEYWORDS`）：5 个**流派资源**（各有 owner），
  和 15 个**通用机制**（蓄火/爆燃/祭火/焚尽/拾回/返照/引爆/刻印/连缀/空明/回响/迟滞/
  反震/烙印/流转）。加牌时优先用通用机制——只有资源和伤害的牌库等于只有一张牌。
- **`pattern` 字段是防重复的关键**，不是装饰。上一版效果同质化的根因是我给的规范太窄
  （"1 费攻击 6~7 伤害"这种基准表），照着填必然一样。现在 `cards.test.ts` 有三条硬检查：
  同牌组同一 pattern ≤3 次且 ≥14 种、整副（含中立）≥16 种、**同牌组内两张牌的 text
  去掉数字后不能是同一句话**。加牌时这三条会直接挡住偷懒。
- **`text` 里出现的机制名必须与 `keywords` 数组完全一致**（有测试，双向比对）。
- 卡牌模块的 import **必须写显式 `.ts` 扩展名**——它要被 node 的 strip-types 跑测试，
  而 strip-only 模式也不支持 TS 的参数属性（这就是 `WorldSoundscape` 要把谱面拆出去的原因）。
- 新增测试文件**必须登记进 `package.json` 的 test 脚本**，否则会被静默跳过。
- **卡面是真实的 Unsplash 摄影**，逐张按牌名与功能检索取图，不是画的（见下）。
  取图表和出处分别在 `src/cards/art.ts` 与 `public/assets/cards/CREDITS.md`。

### 效果设计的依据（重做过一次，别再退回同质化）

第一版效果同质化，根因是我给的规范太窄——"1 费攻击 6~7 伤害、1 费技能 5~6 格挡"
这种基准表，照着填必然一百张牌长得一样。重做时去读了杀戮尖塔 / 万智牌 / Monster Train /
Balatro 的设计资料，下面几条是真正起作用的原则，`cards.test.ts` 已经把能机检的都钉住了：

- **别怕无聊，比怕失败更重要**（Rosewater）。万智牌内部有 Rare Poll：**两极分化的牌
  （有人打 1~2 分、有人打 9~10 分）比人人打 7 分的牌更受欢迎**。九张星陨就该是有人爱有人恨的。
- **"没有废牌"** 是 Mega Crit 公开的目标：每张牌都要在卡池生态里有独特位置。
  填数牌玩家转头就忘，只会记住有"想法"的那几张。
- **转化牌是趣味密度最高的东西**（万智牌的 Body Slam：伤害 = 格挡）。把一个维度的资源
  变成另一个维度，是牌与牌之间的结缔组织。每副牌组至少要有 3 张。
- **状态要简单、可叠加**：一个名词 + 一个整数（余烬/锋锐/灼烧/壁垒/映照都是这样）。
  简单的东西组合起来才有惊喜——3 层灼烧和 100 层灼烧是同一句话。
- **循环牌抬高组合上限**：抽、弃、拾回、返照、回响。牌在手里的流转比数值更能产生连招。
- **"防御很无聊"** 是公认的设计难题，解法是反击 / 格挡转攻击 / 过量格挡的奖励——长明壁垒三条都占了。
- **数值上守 vanilla test**：能力强，身材就要低于基准；否则既强又便宜。
- **构筑锚点**：每副牌组需要 2~3 张"值得为它构筑"的牌 + 一批安全牌 + 少量高风险高回报牌。

## 爬塔地图（`src/battle/map.ts` + `TowerMap.tsx`）

第一章不再是线性推进，而是**7 列 × 16 层 + boss 的塔**（`MAP_ROWS = 16`、`BOSS_ROW = 17`）。
规则照抄杀戮尖塔：6 条路径从 7 列中的 6 列出发、逐层最多横移 1 列、同类不连续、每节点最多
3 进 3 出。**第 1 行固定是教学战 `ch1-1`，最后一排是营火（其中一个改成事件），boss 单节点。**

- **节点只说自己是什么类型，具体打谁等玩家站上去才掷**（`encounterFor`，在 `enterNode` 里）。
  `MapNode` 上**没有** `encounterId`，`map.test.ts` 有一条钉死这点。所以地图页的背景按**类型**
  给（`sceneFor`），不能显示某一场的照片——那是这一层还没做出的承诺。**怪物的数量与血量也在
  开打时才掷**（`spawnRng`）。
- **`MAP_COLS`/`MAP_ROWS`/`BOSS_ROW` 是常量**，UI 的列宽行高从它们派生，别写字面量。
  ⚠️ `TowerMap.tsx` 页头的生命曾经写字面量 `/60`——`run.maxHp` 涨上去就显示成 `66/60`。
- ⚠️ **进边上限的兜底要选「负载最轻的方向」，不能直着走**。写成直着走时，一次实测里 500 张地图
  有 431 张出现某个节点吃到 6 条进边，塔整个歪掉。
- ⚠️ **`boss` 不受进边上限约束**，它是所有路线的汇合点，进边数就等于最后一行的节点数。
  `map.test.ts` 里那条测试显式豁免它——不然会误报。

### 行规则是 **band**，不是逐行锁死

`bandFor(row)` 按段给权重，段内怎么掷是种子的事——「第 8 行全宝箱」那种写法让每张地图一眼读完
**而且长得都一样**。段是：第 1 行战斗 → 2–3 行几乎全战斗（牌组还在建）→ 4–6 行开始出精英 →
7–10 行出宝箱与营火 → 11–15 行全类型 → 最后一排营火。`FIRST_ELITE_ROW` 与 `FIRST_REST_ROW`
**都是 6**，和杀戮尖塔同一个数：前五行是攒牌的地方，营火落在那里是个玩家还没有信息去做的选择。

用 `.work/tower-cadence.ts` 量（DP 枚举**所有**路线，不是抽样）。改完塔应当是这个形状：

```
平均 1.77 个营火 / 条路线     ← 杀戮尖塔约 2
0 个营火   7.0%   1 个 34.5%   2 个 37.5%   3 个 17.1%   4 个 5.1%
中段每行的 rest 占 11–16%      ← 杀戮尖塔掷的是 12%
```

#### ⚠️ 营火曾经只存在于最后一排

`bandFor` 的四条 band 里**一条都没有 `rest`**，所以整座塔唯一能到的营火是 boss 前那一排的保底
火——而那一排是「三条营火 + 一个事件」，踩在事件上的路线**整座塔一觉没睡**。实测 200 个种子：
25.2% 的路线零营火，其余恰好一个，均值 0.75。这就是「爬塔没手感」的真正原因。

补法照抄杀戮尖塔的权重（营火 12% / 精英 8%），把 `rest` 放进上面两条 band。

#### ⚠️ 那个改动当场撞出一个潜伏的生成 bug：营火连营火

`NO_REPEAT`（同类不连续）**只往下看**——它查的是 `from.get(node.id)`，也就是前驱。而最后一排是
`assignLastRow` **在主循环之前**就定好的。于是第 15 行掷点时看不见上方已经是营火，可以连成
两个。这个洞从写下起就在，一直没炸，**只因为 `rest` 从没进过任何 band**，第 11 行和第 12 行
从来没遇上过。`map.test.ts` 那条「同一路径上不连精英/营火/宝箱」在种子 1 上抓到了它。

现在由 `restAllowedOn(row)` 显式禁掉第 `MAP_ROWS - 1` 行——杀戮尖塔有**一模一样**的规则
（「第 14 层不许有营火」），因为它的第 15 层全是营火，理由同一个。

### 打磨（`upgrades.ts` + 卡面）：本局唯一会改变一张牌的机制

打磨**不是另一张牌**，是同一张牌数字变大：`upgrades.ts` 里一行「文案 + 增量」，
`effects.ts` 的效果只写一遍，引擎用 `upgradeFor()` 把增量折进那次执行。
增量形状只有六种（`hit`/`block`/`draw`/`reclaim`/`status`/`power`），**加第七种之前先想清楚**——
需要第七种的升级，通常说明它该是另一张牌。

#### ⚠️ 卡面必须印升级后的文案，否则是在说谎

`CardFace` 原本只读 `card.text`（原始文案），而引擎读 `upgrades.ts`。**两条路读的是不同的文本源，
而玩家只看得到其中一条**：打磨过的砺石印着「抽 1 张牌」实际抽 2 张，三叠印着「2 次」实际打 3 次。

现在 `CardFace` 收一个 `upgraded` 布尔，文案走 **`upgradeFor(card.id, true)?.text`**——和引擎
同一个来源，所以两者不可能再分叉。金色大字 + 名字行右端的「已打磨」角标（`.cq-upmark`：
卡面四角已经有费用盘、品阶、四个括号，只有名字那一行有空位）。

**守这条的测试是 `upgrades.test.ts`**（CLAUDE.md 早先提过它但文件从来不存在，现在有了）。
它不比「文案对文案」或「增量对增量」——那两样在 bug 活着的时候都会通过。它比的是**印出来的数字**
和**那一行的增量**：升级后的文案必须出现一个和原文案不同的数字。砺石就是这条抓出来的。
（「一」算数字：牌库自己混用了「一张牌」和「1 张牌」，同一张牌里就有。）

#### 打磨的两步结算

`repairCard` 应用选择但**不清 `cardTask`**；`dismissCard` 才结束。这是为了让展示屏有地方待：

> **`cardTask` 一清，选牌器就卸载**（分发器顺序见上）。所以「先改牌、再关屏」必须拆成两步，
> 否则改完的那一帧没有东西在渲染。

#### 展示屏：`?` 的 outcome 之后，第二处「玩家可能错过」的地方

打磨完**不停在原地看玩家点「继续」，就什么都没发生**。第一版是 660ms 自动关闭 + 一次金色闪光，
实测玩家压根不知道发生了什么——**看不见的停顿不是反馈，是延迟**。现在：

- 点「打磨」**当场改牌**（`onChoose` → `repairCard`），选牌器不关，变成**单张放大展示**
  （`.nd-reveal`，缩放 1.32，`min-height` 撑住被 `transform` 让出的布局高度）。
- 只有「继续」能离开，**没有任何超时**。
- 打磨过的牌会从 `offered` 里被过滤掉（`!entry.upgraded`），所以**展示屏必须从 `entries` 里
  单独取那一张**——第一版就是因此空屏：玩家一点，那张牌从行里消失，标记和金光全都没地方落。

#### 营火的「这一层就过去了」曾经是假的

营火三选一里，`休息` 走 `campfire()`（会设 `resolved`），而 **`焚牌`/`打磨` 只挂了 `cardTask`**，
`resolved` 一直没设。选牌器是**盖在**营火上面的，所以选完牌回到的是那个从没收起的三选一——
**一次营火可以既焚牌又休息**。

现在两个分支在 `answerCampfire` 里自己设 `resolved: run.at` + 清 `rewardDue`。
**教训：任何「先挂一个子任务、再让任务自己收拾」的流程，都要问一句谁负责结束这一层。**

### 逐层背景：`src/battle/scenes.ts`

**12 个「场景」而不是 12 张遭遇战图**：两场都是狼、都在同一片草里的仗，是同一个地方。遭遇战挂
`scene` 字段，营火/宝箱/事件直接用 kind。

- 地图页的背景**跟着悬停的节点走**，两层交叉淡入（`background-image` 不能过渡，硬切像故障）。
- 取图是 `.work/tower-fetch-scenes.cjs`，**打分方向和遗物那套相反**：遗物要「中央有物件」，
  背景要**暗（均值 0.10–0.62，超出直接丢弃）、低饱和、有质感**。实测 12 张明度落在 0.14–0.39。

### ⚠️ 两个 CSS 坑

- **`.tw-bg > span` 会把装饰层一起选中。** 阴影、颗粒、四角括号都是 `<span>`，被那条规则的
  `opacity: 0` 一起藏掉了——照片和压暗层全部消失，地图直接不可读。照片层现在有自己的类
  `.tw-plate`。**给「只该选中照片」的选择器起个类，别用元素选择器。**
- **压暗必须够狠。** 第一版中段只压到 ~0.45，狼的胸口直接透过节点。现在是一层平铺 scrim 再叠
  方向性渐变，中段合计约 0.78。塔是用 1px 金线和 13px 字画的，压不住就什么都看不见。

## 奇遇（`src/battle/events.ts` + `NodeScreen.tsx`）

塔上的 `?` 层，**24 条**。一条奇遇是一个**有代价的选择**，不是随机礼物：拿生命换金币、拿金币换
遗物、拿一张牌换一张牌。只发东西的 `?` 是一个慢一点的宝箱。

- **三个选项是定数**，文案是**逐条写出来的**（`outcome` 是「你把手伸进去，指尖被什么咬了一口」，
  不是「你失去 6 点生命」）。
- **先付后拿**：`requires` 先查，所以奇遇永远不会是杀死 run 的那件事，也不会让你花掉没有的钱。
  `canAfford` 判生命用的是 **`>`** 不是 `>=`——付完剩 0 血必须挡住。UI 置灰和 `resolveEvent`
  的扣款是两处代码，`events.test.ts` 拿它们对跑。
- **内容长在草原既有设定上**：没到的商队、空心的树、没停下的巡夜、还在烧的火。

### `lean` 是三档，不是两档

原本只有 `gain`/`loss`，**但 24 条里有 16 条是中间的**：有代价，也有不用付的那个选项。
「三张牌，但那张纸你不知道有什么用」既不是净赚也不是亏，塞进哪一档标签就开始说谎。所以：

| `lean` | 机制判据（测试机检） | 现在几条 |
|---|---|---|
| `gain` | 有选项白拿 | 5 |
| `cost` | 有代价，**也有**不付代价的选项 | 15 |
| `loss` | 每个选项都要付，或者白跑 | 4 |

**「只发东西的 `?`」只有 5 条，这是有意的**：多数 `?` 都该是一个问题。

### `eventForNode` 由节点 id 推导，**不掷点、不存档**

奇遇的身份要经受住重绘、重载和「同一个 screen 渲染一百次」，而 run 的 `rng` 流每调用一次就前进
一格——**用 `rng` 选奇遇会在每次重绘换一个事件，还会顺带把整条 run 的确定性搅乱**。所以用
FNV-1a + xorshift 把 `nodeId` 哈希成下标：同一个楼层永远是同一个奇遇，什么都不用持久化。

因此它**不是 `enterNode` 的事**：这里没有任何「玩家走进来才决定」的东西——玩家走上的就是「奇遇」，
这就是站在那儿的那件事。（对比 `currentFight`：那个必须存进 run，因为它要在准备页、战斗和重载
之间保持一致。）

### ⚠️ 结果屏必须分两步，这是踩过的坑

`EventScreen` 原本点一下就 `onChoose` → 立刻 `setRun`。而 **`run.resolved` 一置上，分发器就不
再把这一页当奇遇屏了**（`BattleDemo.tsx` 的顺序是 cardTask → pendingReward → floor），于是：

> 玩家点「徒手挖开」，一帧之后看到的是**遗物抽取**。**写给这个选项的那句 `outcome` 一次都没渲染过。**

`EventScreen` 里那个 outcome 分支从写下来就是死代码。现在拆成两步——**先看结果，再按「继续」**，
`onContinue` 才把答案交给 run。`EventScreen` 因此持有「已答但没提交」的本地状态，**跨楼层要给
`key={floor.id}`**，否则爬到下一个奇遇时它还记着上一个的答案。

同类问题以后还会遇到：**任何「先提交 run 状态、再显示反馈」的节点屏都会这样被吞掉。**

### 代价要印出来

`hint` 说的是楼层自己的话（「路很远」「土是热的」），**故意不是数字**——所以数字得从别处来。
选项上有一行 `需要 30 金币`，由 `option.requires` 现算。少了这行，置灰的选项读起来像 bug，
不像一个你暂时付不起的价格。

## 遗物层（`src/relics/`）

`#/relics` 是遗物总览页（验收用）。和 `src/cards/**` 一样，**整块不 import `src/world/**` 或
`src/battle/**`**——看牌改牌碰不到地图、run 或存档。

**71 件，五阶：残片 → 旧物 → 珍品 → 秘宝 → 绝响。** 阶梯量的是「一件东西在长夜里活了多久」，
所以**故意不沿用卡牌的火焰阶梯**（残烬 → 星陨）——两条都叫「更稀有」、都听着像在烧的阶梯，
是多余的一条。件数是 14 / 22 / 16 / 12 / 7：**中段最宽是有意的**，杀戮尖塔的遗物也是
普通 24 / 罕见 30 / 稀有 21，玩法真正发生在中段。真正的阶梯是**权重**（34/30/20/11/5）。

- **设计原则：每一件都必须咬在已经存在的机制上**（余烬/锋锐/壁垒/映照/灼烧/烙印/反震/蓄火/
  连缀/空明），而不是给数字加数字。写着「+2 伤害」的遗物，是一张忘了自己该有趣的牌。
- **主槽给卡面上半部分的完整效果，副槽给下半部分的折扣效果。** 副槽文案是**逐件写出来的**，
  不是按公式缩放——「灼烧不再递减」没有一半版本的自己，它有的是另一个版本。
- 第一章放出前两阶 + 若干珍品（共 **52 件**），`秘宝` 与 `绝响` 留给后面的章节。

### 取图：必须搜到「物件」，这是这个脚本和前一个的唯一区别

`.work/relics-fetch-photos.cjs` 与卡牌那套同构（真 Chrome、CDN 取已裁切已编码的 webp、
缩略图打分、三标签页并行、可断点续跑），但**多了一项「中心聚焦」打分**，而且这项是必需的：

> 关键词搜索会老老实实把 `compass` 答成一张日落海平线、把 `mask` 答成一张有人戴着口罩的照片。
> 卡牌那套的打分（饱和度 + 层次 + 边缘密度）**恰好奖励这两者**——日落又饱和又满是边缘。
> 所以这里额外算一个 `focus`：**画面中央那半张的边沿能量占整幅的比例**。桌上的物件很高
> （大部分画面就是它），海岸线很低（细节是横贯整幅的一条），人脸居中但过亮过平，层次项已经不喜欢它了。
> `focus` 作为乘数压分，低于 .5 的会被标 `~ weak` 打印出来供人工复核。

实测有效：**初次批量 71 张里约 27 张不合格**（艺术照占位、白底过曝、以及一张 `ember-heart`
搜到了人举着相机的照片）。逐条改成更字面的说法后重取，剩 5 张，再一轮清干净。

- **重取一批用逗号分隔的 id**：`node .work/relics-fetch-photos.cjs "a,b,c" --force`，
  一次浏览器会话跑完，不要一件起一个 Chrome。
- **改检索词走 `.work/fix-relic-queries.cjs` 这种脚本文件，不要用 `node -e` 内联正则**——
  反斜杠和引号过 shell 会被吃掉，静默匹配 0 条然后你以为改了。这个坑踩了两次。
- `relics.test.ts` 有一条**双向**校验：每件遗物都必须有检索词（否则是一张没图的牌），
  每个检索词都必须有对应的遗物（否则是一张没人要的照片）。另有一条拦风景/人像词。

**改遗物后务必重新截图核对**（`.work/relic-page-shot.cjs`），别凭推断下结论。

### 卡面：`rl-` 前缀，和扑克牌刻意不同

`.work/relic-page-shot.cjs` 出图。参照物是**三国杀装备牌**：物件在框里、规则印在下面。
和 `cq`（扑克牌）的区别是**双线画框**（外粗内细）而不是单层斜切——这一条是「被携带的东西」
和「拿在手里的东西」的视觉分界。另外遗物**没有费用角标和类型行**（它永远不进手牌），
换成顶部的**品阶绶带**。

**文字块是固定高度、顶部对齐的**（`.rl-body { height: 172px }`）。贴底绝对定位的话，
副槽文案一长就把卡名顶上去，一整排卡名会参差不齐像楼梯。

## 玩家存档

`eternal-night-world-v1`（localStorage）。地形布局变更后旧存档会失效（坐标对不上），
新存档不受影响。

## 卡牌美术的取图管线

`.work/cards-fetch-photos.cjs [deck前缀|all] [--cards-only] [--force]`

Unsplash 的搜索接口有机器人防护，所以脚本**驱动真 Chrome**（curl 和 npx 都不行）。
图片直接从 CDN 取**已经裁成卡面比例、已经编码成 webp** 的版本，因此本地不需要任何图像处理
（系统里的 `convert` 是 Windows 的磁盘工具不是 ImageMagick，Python 是商店占位版）。

- **关键词搜图会返回大量垃圾**（纯色渐变、平面金属贴图、黑白快照），所以每张候选先用缩略图
  在浏览器里算饱和度/层次/边缘密度打分，取最高分的那张下全尺寸。没有这一步，
  大概一半的卡面是不能用的。
- 三个标签页并行 —— 每张牌要一次搜索导航，导航就是全部成本，串行要跑一个多小时。
- 会跳过已存在的文件，可断点续跑。改 `art.ts` 里某个检索词后，删掉对应 webp 再跑即可。
- `.work/cards-contact-sheet.cjs <tag> [格宽] [目录] [扩展名]` 把整个目录拼成一页截图，
  一次 Read 就能看几十张图。注意它把图片**内联成 base64**——`setContent` 出来的页面是
  about:blank 源，Chrome 会拒绝加载 `file://` 子资源。

## 战斗层（`src/battle/`）

`#/battle` 是第一章的**完整 run**（含新手引导）。和 `src/cards/**` 一样，**整块不 import `src/world/**`**。

页面有三个相位，**由 `run` 与 `state` 两个 state 推导，不另设 `screen` 标志**（早先草案里设了，
它会和它描述的状态打架）：

- `run === null` → **章节开场**，选主副朝向
- `run !== null && state === null` → **战前准备**，看本场遭遇战、调换主副、开战
- `state !== null` → **战斗本体**（原有那套 FX/日志/引导架构完全没动），结束后叠战果面板

### 一条 run 的规则（`run.ts`）

- **血量跨战斗继承**，每场胜利后**随机回血 9–15**（上限 60）。回落进 `rng` 这条私有流，
  战斗洗牌走 `battleSeed(run, id)`，两条流分开，所以同一个 run 种子能重放出完全一样的一整章。
- **牌组对整章锁定**，但**每场开打前可以调换主副**。第一章两副牌就是 `['blade','bone']`，
  锁的是这个「对」，`swapDecks` 只交换这两者。
- **`cleared` 是一本胜利流水，不是主线顺序**。塔上的仗从池子里抽，池子里有章节锚点，
  所以打赢的顺序**和章节顺序无关**。`isValidRun` 现在只查**成员资格 + 去重**，不再查前缀。
- 页头/战果里显示的**必须是正在打的那一场**（`state.encounterId` 反查），不是
  `currentEncounter(run)`——胜利会立刻推进 run，问 run 会把刚打完那场标成下一场的名字。

#### ⚠️ 「通关」曾经把整章吃掉

`isChapterCleared` 原先是 `cleared.length >= 5`。`cleared` 是**追加日志、不去重**，而池子里有
`ch1-3`（精英锚点）和 `ch1-4`（头狼）——**同一场可以打第二遍**，第二遍又追加一条，长度凑到 5，
章节就宣称结束了：

> 准备页换成「这一章的五个夜晚都过去了」、**开战按钮禁用**。玩家爬到 boss 层，
> 这一局**再也打不了 boss**。而重载反而正常（`isValidRun` 的前缀规则把这份存档丢了）——
> 所以它只在当前会话里出现，表现就是「每次点到 boss 都直接给我看通关画面」。

修的是三处，缺一不可：

1. **`isChapterCleared` 改成「五场章节战**每场**都打赢过」**（`every` + `includes`），不是数长度。
2. **`finishBattle` 不重复追加**——同一场打赢两遍只记一次。不去重的话这份存档重载时会被
   `isValidRun` 丢掉，玩家刷新一下整章就没了。
3. **`isValidRun` 把「前缀」换成「成员资格 + 去重」**。前缀规则当年是防跳关的，现在由
   成员资格来防（不在章节里的 id 一律拒），去重来防「五条里有一条重复」。
   **代价是前缀规则会误杀合法的一局**——打赢顺序本来就可能是乱的。

顺带：`encounterFor` 现在**优先排没打过的**遭遇战。重复的胜仗 `finishBattle` 是空操作
（不加牌不加钱），一整层白走，而池子经得起这个偏好（只有池子打空了才回落到重复）。

#### ⚠️ 别用 comparator 做「把打过的排到后面」

`encounterFor` 里那个偏好一开始写成 `[...pool].sort((a,b) => Number(beaten.includes(a.id)) - Number(beaten.includes(b.id)))`。
**这个比较器不合法**：两个都没打过返回 0，一新一旧却返回非 0，顺序取决于参数先后——不是一致的
排序关系，V8 有权还你一个没分好组的数组。它还真还了：`w-ashdogs` 大约每八次就有一次排到
`ch1-1` 前面。改成 `filter` 分两组再挑，没有排序语义可依赖。**这个坑是测试抓出来的**——
单跑一次是好的，64 个采样点扫过去才现形。

### 主副牌组怎么合成（`chapter.ts` 的 `chapterDeck`）

引擎洗的是**一副实体牌堆**，所以 80/20 必须落成牌堆构成，不是每次抽牌掷点（两者统计等价，
但前者零引擎改动）。**主牌组整副保留，副牌组每张取 1 份、按「签名牌优先」取到约 20%**。

- 「签名牌」= `keywords` 含该副牌**自己拥有的资源**的牌。注意 `RESOURCES` 里 **bone 拥有两个**
  （锋锐 + 壁垒），所以判据是集合不是单个 id。
- **张数用 `splashSize()` 按「占比最接近」反解，不要直接算除法**：`1 - MAIN_SHARE` 在浮点下是
  `0.19999999999999996`，直接除会把 22 张的主牌组算成 18.5% 的掺入（5 张而非 6 张）。
- 结果：刃主/骨副 28 张（副 6），骨主/刃副 30 张（副 6）。

- **第一章对应地图里第一片解锁的地貌**：草原 · 余烬营地（THE LAST HEARTH）。怪物图鉴要从
  地图的既有设定里长出来——草原的敌对动物是灰狼，隐秘据点是萤火树洞，营地的关卡是
  荒野巡夜 / 萤火树洞 / 失落的商队 / 草甸上的头狼 / 草原守望者。**不要自己另起一套设定**
  （早先误做成「灰烬墓园」，那是弃用原型里的东西，地图里没有）。
- **怪物设计原则：每只怪逼玩家回答一个不同的问题**。两只血条加一个伤害数字什么也教不了；
  「先杀小的」或「别攥着资源结束回合」才教得会东西。九只怪各自的题目写在 `enemies.ts` 的开头。
- **首领机制是整章的论点**：断罪之刃和长明壁垒都靠攒引擎取胜，所以「余烬守望者」的换岗把
  一张对你有利、对它更有利的牌塞进你的牌堆——问题不再是「我能不能转起来」，而是「值不值」。
- 只有第一章解锁的 24 张牌有实现（`effects.ts` 的 `CARD_EFFECTS`）。其余牌是**上锁状态**，
  图鉴里显示但抽不到。加新卡时先加进 `chapter.ts` 的 `unlocked` 再写效果。
- 引擎是纯函数 + 确定性种子，124 项测试。`intentFor()` 会算出聚群等加成后的真实数值，
  **UI 必须用它而不是直接读 `enemy.intent`**。
- `startBattle(encounterId, deck, seed, opts)` 的 `opts = { hp?, sub? }` 是 run 与引擎之间
  **唯一**的接口。**不要在 `BattleState` 里加 run 字段**：`isValidBattle` 钉死了
  `maxHp === PLAYER_MAX_HP`，带血开局本来就合法，而 run 的状态属于 `run.ts`。
- 引导**只在本次 run 的第一场**开打时弹（`TOUR_KEY` 之外再加一道 `cleared.length === 0`），
  否则每场都弹一遍。

### 下一轮：boss 击破解锁（本轮明确没做）

`CHAPTER_1.nextUnlock` 承诺击破守望者后解锁**明焰阶四张/副**，但**这 8 张牌的效果一张都没实装**
（`effects.ts` 只有那 24 张）。直接解锁会让玩家拿到 8 张白板，所以本轮只把文案改成如实描述。
要做完得先写效果，其中 **「照壁」的「回响」要现设计**——「把这张牌的一个弱化复制品加入手牌」
目前没有对应机制，已实装的 24 张里没有任何一张用过 `echo`。七张是直活
（`EffectContext` 的 `spend`/`stacks`/`hitAll`/`bank` 够用）：
`blade-16` 断罪 / `17` 刃雨 / `19` 裂甲 / `20` 借焰、`bone-16` 回震 / `17` 照壁 / `19` 炭墙 / `21` 封炉。

### 遭遇战随机化：种类固定，数量与数值掷点

- **`EnemyDefinition.hp` 是区间 `[min, max]`**，不是单值。数值带以原来那个单值为中心，所以之前
  调好的平衡在期望上仍然成立。掷点走**独立的一条流**（见下），不破确定性。
- **`Encounter.units` 是 `{ id, count: [min, max] }[]`**：**种类固定，只有数量掷点**。这是承诺
  「你会遇到什么」而不是「会遇到多少」，玩家能规划一场仗的形状，但背不下它的数字。
  `荒野巡夜` 是唯一不掷的（教学战的棋盘不能在文案底下变）。
- **`POOLS` 是地图要抽的池子**（`weak` / `strong` / `elite` / `boss`），五个锚点就**坐在池子里**，
  不是另开一份。`startBattle` 认的是 `ALL_ENCOUNTERS`，所以池子里每一场现在就能开局、能被测。
- **异变（`MUTATIONS`）**：小兵与寻常怪 12% 概率带一个词缀，名字加前缀、颜色换掉、徽章写「异变」。
  这是「燃烧精英」的小尺度版——一场你以为熟悉的仗里多了一个必须注意到的东西。**精英与首领永远
  不异变**，它们本身就是那个「必须注意到的东西」。
- **`defOf(enemy)` 是唯一该读敌人定义的地方**：它把异变折进基础定义。直接读 `ENEMY_BY_ID`
  会让「披甲的」只剩名字和颜色——它设的 `regenBlock` 会被静默丢掉。

**`spawnRng` 必须和 `rng` 分开，这是架构不是洁癖。** 怪物血量、数量、异变都走 `spawnRng`；
`rng` 只是**洗牌流**。合并成一条会立刻出问题：掷点的消耗会改掉洗牌顺序，于是「换岗」那条测试
（断言抽牌堆里有誓言）会因为一只狼掷到「强健的」而挂掉。杀戮尖塔也是在**战斗生成阶段**掷怪物血量的，
牌堆那时还不存在。有一条测试钉死了这一点：同种子同牌组、不同遭遇战，抽牌顺序必须逐张相同。

### 遗物接进战斗（`src/battle/relics.ts`）

**数据与行为分家，和卡牌完全同构**：`src/relics/relics.ts` 是数据（名字/品阶/两套文案），
`src/battle/relics.ts` 是行为，正如 `src/cards/*.ts` 与 `src/battle/effects.ts` 的关系。
行为文件**不 import 引擎**——引擎把 `RelicContext` 递进去，依赖单向（`engine → relics`）。

**`BattleState.relics` 存的是 id，不是处理器。** `clone` 是 `structuredClone`，每个入口都克隆，
函数存进去会被扔掉。行为和卡牌一样靠查表。

三种形态，别硬塞成一种：
- **triggers** 到点触发（battleStart / turnStart / turnEnd / enemyKilled / firstAttacked / firstAttackedTurn）
- **modifiers** 引擎来问数字（attackDamage / scorchDamage / drawCount / maxHp / kindling / 费用 …）
- **flags** 改的是规则本身（lethalSave / retainFirstCard …）

**flag 是布尔的，但效果不是。** 所以引擎问的是 `relicFlagSlot(s, flag)`「这条规则**来自哪个槽**」，
强度随槽位走：燧发枪弹副槽那一击减半、铁面具副槽救完收 1 点能量、双生镜副槽只接攻击牌。
写成 `relicFlag()` 的布尔判断会让主副槽一模一样。

**`[n, n]` 是错的。** 修正器如果主副槽写成同一个数字，副槽等于没打折。`relics.test.ts` 有一条
结构检查专门挡这个——行为对比试过，被一堆「条件没满足所以看不出差别」的遗物淹没，判不出真问题
只产噪声。

### ⚠️ 引火会把「费用类遗物」盖住

引火（本回合第一张牌 −1）和所有减费遗物（断齿梳、铁匠锤、雷击木）**叠在同一个位置**，而
`cardCostNow` 最后会 `Math.max(0, cost)`：**一张 1 费牌当回合第一张打出去，装不装减费遗物都是 0 费**。
所以这些遗物在实战里经常「看起来没生效」——玩家第一张就出技能牌，永远看不到差别。

要观察它们，必须让**不是首张**的牌（或 2 费以上的首张）去吃这个折扣。`relics.test.ts` 的脚本
因此刻意固定了出牌顺序（先 2 费、再能力牌、再技能牌，每回合只出两张），而不是贪心地打完。

**这不是 bug，但这个重叠是真实的手感问题**，将来调平衡时值得回头看。

### 遗物全量测试：`relics.test.ts` 的四条

1. **凡能抽到的必有实现**——挡住「抽出白板」
2. **每一件装上后都必须真的改变什么**——跑一场固定脚本的战斗，逐字段对比
3. **副槽同理**
4. **登记在案的那几件，换场景点名验**

第 2/3 条有一个 `NOT_VISIBLE_IN_ONE_BATTLE` 白名单（胜利结算类、需要灼烧/灰烬/濒死的、需要特定
回合的）。**这张表不是藏东西的地方**：一件遗物停止工作会立刻以失败出现，逼你解释或修复它。

> 加这张表时逐件查过，**没有一件遗物是真的坏的**——17 件「没变化」全是场景没覆盖到。
> 但抓出两件真问题：**蜡头副槽写的「若手牌少于 4 张」永远不会触发**（起手恒为 5 张），
> 以及**攒烬的 `type` 是 `skill` 而不是 `power`**（它装的是能力，但印的类型是技能）——
> 后者会让「铁匠锤减第一张能力牌」在任何只出攻/技的脚本里显得完全没用。

### 挂钩点位（改引擎前先看这张表）

| 漏斗 | 位置 | 说明 |
|---|---|---|
| `strike` | `makeContext` 内 | **攻击伤害的唯一正确落点**：`hit`/`hitAll` 都走它，每次命中各算一次，且避开反震与亡语 |
| `dealToEnemy` | | 所有吃格挡的敌方伤害 |
| `damagePlayer` | | 敌人打你；**减玩家血有三个地方**（这里 / `loseHp` / 玩家灼烧），不是一个 |
| `cardCostNow` | | 费用唯一的读点。⚠️ `BattleDemo` 的手牌显示曾经用 `cardCost`，改费用会让它说谎 |
| `settle` | `enemy.dead = true` 处 | 敌人死亡的唯一观测点 |
| `startBattle` | `beginTurn` **之后** | 见下 |

⚠️ **`battleStart` 必须在 `beginTurn` 之后触发，不是之前。** 两条都是实测撞出来的：
铜镜要「复制手牌中随机一张」，而 `beginTurn` 之前**没有手牌**；更要命的是 `beginTurn` 开头会清空
反震（它只活一个完整回合），所以「战斗开始时获得 1 层反震」会在玩家看见那个数字之前就被抹掉。

⚠️ **改 `maxHp` 撞了 `isValidBattle`**：原来写死 `maxHp === PLAYER_MAX_HP`，现在放宽成
`>= PLAYER_MAX_HP`（守望者之誓要抬高上限），**但降低仍然非法**——60 是所有常数调过的基准。
连带 `run.ts` 的回血封顶也改成跟着 `state.player.maxHp` 走。

⚠️ **`energyPerTurn` 被 `isValidBattle` 钉死**，所以「每回合 +1 能量」只能加在 `beginTurn` 的能量
表达式里（`energyBonus`），不能改那个常数。

### 解锁与抽取

- **元进度，不属于 run**（`src/relics/unlocks.ts`，key `eternal-night-relic-unlocks-v1`）。
  开局 36 件（残片+旧物），**击破 ch1-4 小首领解锁 16 件珍品 → 52 件**，跨周目保留。
  放进 run 里会每次重置，那正是它要避免的事。
- **每打赢一场有等级的战斗**（`GRADED_POOLS = {elite, boss}`）给一次抽取；主槽抽完 **25%** 概率再开副槽。
  ⚠️ 「有等级」按的是**池子**不是章节顺序——塔从池子里发牌，不是线性推。
- ⚠️ **走到第 `FIRST_RELIC_ROW`（3）层还没拿到遗物的，赢一场补一件**（`finishBattle`）。
  不加这条时：**21.8% 的路线走完整座塔一个精英都遇不到**（200 种子实测；把精英下限从第 6 行提到
  第 4 行也只压到 16.2%），那些局在打 boss 之前一件遗物都没有，于是**淬炼类的奇遇对它们是一扇
  锁着的门**。判据是 run 自己的 `relicGranted` 标记，**不是「`relics` 是否为空」**——用后者的话，
  玩家把不喜欢的遗物丢掉就能再刷一件。
- **装哪个槽是玩家的选择**（`claimRelic(run, id, placeIn)`）。早先写成「第一次抽进主槽、第二次进副槽」，
  玩家只是看着一个仪式走完，什么都没决定——**战斗准备页还能调换主副、丢弃某一件**
  （`swapRelics` / `discardRelic`），否则一件拖累你的遗物（比如断刃的费用税）带上就再也放不下。
  抽取面板上会写明目标槽里原本是什么、会不会被换下来。

### 遗物槽的界面位置

**战斗中在手牌那一行的最左边**，用**完整卡面**（`.bd-hand-relics`）。放这儿是因为遗物和手牌是
同一层的东西——都是「你此刻手上有什么」——而且只有这一层放得下 240 宽的卡面。角标「主/副」
说明卡面上哪一半是生效的，所以卡面本身不需要在主副槽之间变形。准备页的同一组槽是**可操作的**
（多出「丢弃」和「调换主副」），战斗里只读。

⚠️ **遗物轨必须包在 `.bd-floor` 里，不能直接放进 `.bd-hand-row`。** 手牌行是
`overflow-x: auto`，而**这会让 `overflow-y` 也变成 auto**（`overflow-y: visible` 与
`overflow-x: auto` 不是合法组合）——挂在行上方的遗物会被整个裁掉，元素在 DOM 里、量得到尺寸、
`elementFromPoint` 却命中的是日志。包一层 `position: relative` 的 `.bd-floor`，
遗物轨在桌面端绝对定位覆在手牌行左侧，窄屏则变回手牌行上方的一个普通行。

⚠️ **别为了排版给 `.bd-hand-row` 加 `flex-wrap: wrap`。** 手牌的重叠靠负 margin，
**只在单行里成立**；一换行整手牌会竖着堆成楼梯。

### 爬塔页的滚动与背景：两个位置相关的坑

- **滚动必须交给整页（`.tw { overflow-y: auto }`），不能交给塔那一列。** 塔和右侧信息面板
  是并排两列，滚动塔那一列会把**它的滚动条留在屏幕正中间**（列在那里结束）。让整页滚，
  滚动条才回到窗口右缘；页头 `sticky`、面板 `sticky` 挂在页头下方（高度用测出来的
  `--tw-head`，因为页头在窄屏会换行）。
- **整幅背景用 `position: fixed`，不能用 `absolute`。** 根容器一开始滚，绝对定位的背景就只
  覆盖**第一屏**——往下滚会露出底下的空白页。`fixed` 顺带让背景在塔滚动时保持不动，
  这本来就是想要的观感。

### 压暗的度

`--tw-shade` 的中间值试过两端：**0.45 太亮**（狼的胸口直接透过节点，路线看不清），
**0.92 太暗**（12 张照片等于白取）。现在约 **0.84**——地方还看得出是个地方，金线也还压得住。
- 抽取走 run 自己的 `rng` 流，所以整条 run 由一个种子完整重放（含金币与回血）。
- **金币是这一轮顺带加进 run 的**：铜板与商队账本只影响金币，而 run 里原本没有金币系统，
  那两件就是白板。

### 头狼：一次「比 boss 难四倍」的返修

第一版头狼是 80 血 / 9 攻 / 每 3 回合召 2 狼并永久 +2 力量，循环无限。用固定策略实测：

| 遭遇战 | 刃主胜率 | 骨主胜率 |
|---|---|---|
| ch1-1 / ch1-2 | 100% | 100% |
| ch1-3 精英 | 100% | 80% |
| **ch1-4 头狼（改前）** | **20%** | **0%** |
| ch1-5 boss（血是它两倍） | 100% | 40% |

**小首领比首领难四倍，就是设计坏了。** 伤害曲线是 9 / 0 / 33 / **54**——第 4 回合 54 点，而玩家
只有 60 血。关键教训：**扫血量几乎没用**（70→40 血胜率只从 20% 动到 40%），因为伤害从来不是
头狼打出来的，是它**不断补满的场面**打出来的。两条真正的原因：

1. **聚群按全场计数**。4 个目标时每只影狼的聚群变成 +6，咬 11——比它身后的首领还狠。
   现在 `packBonus` **只数同类**：`「同伴」是同伴，潜草者不是影狼的同伴`。这一条单独就把总伤害
   从 Σ96 压到 Σ84。副作用很好：两种狼型分工清楚了，影狼靠自己人数量成长，头狼靠发力量。
2. **嚎叫每 3 回合循环 + 永久叠力量**。现在**只响一次**——这也正是教学文案一直在承诺的
   （「要么在**那**之前解决它」是死线，不是节拍器），于是这仗变成一个真选择：抢杀，还是花两回合清狼。

同时 `ch1-4` **去掉了潜草者**：它给这场仗装了**第二个**「不处理就死」的时钟，两个时钟不是更难的
仗，是读不懂的仗。而嚎叫召来的狼本来就是设定承诺的场面（「它一开口，草里就会站起来更多东西」）。

改后：**刃主 100% / 骨主 80%**，与精英战持平，平均 6.1 回合。头狼血（42-50）比精英（43-53）还低，
因为那两只狼才是它威胁的大头——这是有意的差异化，不是笔误。

**改数值务必用固定策略量胜率再下结论**，别凭感觉：这次的曲线和那张表才是把病因从「血太厚」
纠正到「场面补太快」的东西。

### 能量：3 点是基础，第 4 点靠「引火」打折给

「3 点不够用、4 点又太多」的根因**不是能量数值，是长明壁垒被解锁名单砍掉了全部 0 费牌**：
它本来设计了砺石（`bone-05`）与殉道（`bone-09`），两张都没进第一章，于是它成了唯一
「最便宜 1 费」的牌组——3 点能量永远刚好等于 3 张牌，5 张手牌必然卡住 2 张，而「连缀」
要求一回合打出 ≥3 张，等于每次都把能量全押上才够得到。断罪之刃有 9 张 0 费牌，根本感觉不到。
**这两张牌已放回 `CHAPTER_1.unlocked.bone`（骨主牌组 24 → 28 张）。**

在这之上加了 **引火（`KINDLING_DISCOUNT`）**：每回合第一张牌费用 −1，最低 0。

- **它不需要新状态字段**：`playedThisTurn === 0` 就是「本回合第一张」。注意 `playCard` 里
  **必须在 `playedThisTurn += 1` 之前读费用**，否则每张牌都会悄悄吃到折扣。
- **它自己会平衡**：实测骨主 2.98 → 4.02 张，而刃主 4.43 → 4.49（几乎不动，因为它本来就用
  0 费牌起手，减 1 无意义）。这正是「3 和 4 之间」。
- **不要改成基础 4 点**：断罪之刃每回合会**浪费 1.02 点能量**（1/4 白给），那就是「4 点太多」
  的量化来源。杀戮尖塔里 +1 能量从来不是基础值，而是**带代价的遗物**（咖啡壶：+1 能量、
  不能休息）。将来做遗物时照抄这个原则。
- 界面在能量点旁挂了 `bd-kindling` 徽记：亮 = 折扣还在，灰虚线 = 本回合已用掉。

### `battle.css` 的按钮重置必须带 `:where()`

原来是 `.bd button { ...; background: none; border: none; }`，权重 **(0,1,1)**，**压死了下面每一条
单类规则**：`.bd-btn` 的边框、`.bd-end-turn` 的渐变底、`.bd-tour-next` 的背景全都写了却没生效，
只有两个类的 `.bd-sound.on` 权重够、活了下来——所以整页只有音效按钮长得像按钮，
「结束回合」一直是裸文字。现在写成 `.bd :where(button)`，权重归零，
重置才真的只是重置。**加按钮样式时别再写 `.bd button` 这种带元素选择器的重置。**

### 怪物立绘：自动抠图不成立

试过三轮（亮度羽化 → 硬边边缘泛洪 → 加质量闸门拒绝破洞主体），9 张里只有 2~3 张干净。
原因：亮度阈值分不清「暗」和「外面」，只对**纯黑棚拍**有效，而狼和篝火不是棚拍。
**别再走这条路。** 现在用的是**拱形画框 + 暗角**承载整张照片（`.work/monsters-portraits.cjs`），
任何照片都成立，也更像怪物位而不是贴纸。取图和评分逻辑与卡面同一套（暗调优先）。
